version: "3.4"
services:
  web:
    image: konnorsccot/appclass:latest
    ports:
      - "5000:5000"
    networks:
      - webnet
  redis:
    image: redis
    networks:
      - webnet
networks:
  webnet: